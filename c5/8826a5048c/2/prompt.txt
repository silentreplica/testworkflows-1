Create the following files with EXACTLY the content shown (byte-for-byte). Do not add, remove, or change anything. Do not explain.

File: /tmp/aigov-reallife-542681295/repo/api.ts
Content:
```
export async function fetchData(url: string) {
  return fetch(url).then(r => r.json());
}
```